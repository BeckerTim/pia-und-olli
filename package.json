{
  "name": "pia-und-olli",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev . --r2=PHOTOS",
    "deploy": "wrangler pages deploy ."
  },
  "devDependencies": {
    "wrangler": "^4"
  },
  "dependencies": {
    "aws4fetch": "^1.0.20"
  }
}
